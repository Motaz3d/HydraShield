# Talaix — Working Plan

Living plan between the operator and the AI copilot. Statuses are kept
honest: an item is "done" only when it is verified in production.

Legend: ✅ done · 🔶 partially done / in progress · ⬜ planned

## Platform foundation

| Item | Status | Notes |
|---|---|---|
| User registration end-to-end (register → email verify → login) | ✅ | Verified live 2026-08-22: 201 on register, real verification email via Gmail SMTP, browser redirect to `account.html?verified=1`, session persists across deploys (`HYDRASHIELD_SECRET_KEY` set) |
| Operator account `info@talaix.com` = admin | ✅ | Activated + promotion chain verified server-side; Commercial Center at `admin.html` |
| Transactional email (verification, reset, alerts) | ✅ | Gmail SMTP live on Vultr (`/opt/hydrashield/.env`, never in Git) |
| SMS alerts | 🔶 | Code complete, 33 tests green, three honest backends (outbox/http/twilio in `src/dashboard/sms.py`); production runs the dev outbox. 2026-09-10: Twilio account created (upgraded, not trial — $20 balance + free SMS units). 2026-09-10 ~08:49: **Business Profile submitted** (regulatory bundle `BU680c3fcfcba6aa05517930c9d49ac8fb`, account `AC393f…5023`) — **REJECTED same day 17:16** (5 errors: 18606 email≠website domain — motaz3d@gmail.com was used instead of @talaix.com; 18604 authorized-rep identity unverifiable; 18603 address not in authoritative DB; 18602 business ID unverifiable — **structural: pre-incorporation, no legal entity exists**; 18601 business-name↔website association unverifiable). **Fix plan (2026-09-10)**: never fabricate a business ID — fix what's fixable (info@talaix.com everywhere for domain match, exact national-postal-format address, rep = founder + info@talaix.com), then either recreate the Trust Hub profile as **Sole Proprietor** (no business-ID path) if the console offers it, or ask trusthub-verify@twilio.com which profile fits a pre-incorporation project (support email drafted in session), or defer Twilio to post-incorporation; production unaffected (dev outbox). **Wiring pending**: after approval buy SMS-capable number in console, then set `SMS_PROVIDER=twilio` + `TWILIO_ACCOUNT_SID`/`TWILIO_AUTH_TOKEN`/`TWILIO_FROM_NUMBER` in Vultr `/opt/hydrashield/.env` (never in Git) + `docker compose up -d`; manual .env edits survive redeploys (set_env never overwrites non-empty values) |
| Show/hide password on auth forms | ✅ | Live on `account.html` |
| Navbar Account pill — fragmented-border bug | 🔶 | 2026-09-08: crowded flex nav squeezed `.nav-account` below the pill's natural width; the inline anchor's end-padding wrapped to a 2nd line box → border drew as broken bracket fragments on `account.html`. Fixed in `website/css/style.css` (`flex-shrink:0` on the li + `white-space:nowrap` on the anchor); verified via headless-Chrome renders 1440/1280/375 — **local only, not yet committed/deployed**. Known pre-existing issues found (unfixed): whole nav overflows viewport ~800–1100px (hamburger only ≤768px); account plans table overflows horizontally on mobile |
| Tropical cyclones — 7th hazard | ✅ | GDACS (UN-OCHA/EU JRC) live global monitoring: analyze + active-storms map layer; historical tracks via IBTrACS declared candidate (not wired, honestly stated) |
| International trade movement on the map | 🔶 | Ports/harbours layer live (OSM, 50 km, `/api/trade-infrastructure`); live vessel tracking (AIS) needs a shipping-data provider — declared, not wired |
| AGENTS.md project map + session protocol | ✅ | 2026-09-05: root `AGENTS.md` maps the repo + auto session protocol (read PLAN.md at start, update both files at end) — saves exploration tokens every session |
| Analytical engine → public open-source repo (`tore`) | ✅ | 2026-09-06: engine extracted to https://github.com/Motaz3d/tore (EUPL-1.2, "Talaix Open Risk Engine") for the NLnet Restack application — tx_core + src.climate (minus web blueprints) + src.prediction + src.gis_mapping + analytical dashboard data-pipeline + hydration_control + 4 config registries. 34 tests green in standalone venv; verified live end-to-end (8/10 hazards real data at Mersin; dust/volcanic honestly unavailable). **Binding: engine changes must be mirrored — `scripts/sync_tore.sh --push -m "..."`** (rule in root AGENTS.md) |

## Geographic coverage — "we cover the entire Earth"

| Item | Status | Notes |
|---|---|---|
| Global coverage statement on homepage | ✅ | Hero states global coverage with the honest terrain caveat |
| Actual data coverage | ✅ | Core integrated datasets are global (Sentinel-2 + Landsat C2 L2 fallback, Open-Meteo, ERA5/-Land, FIRMS VIIRS/MODIS, WorldPop, OSM, GloFAS, ESA WorldCover); elevation 25 m Europe / 90 m for 60°N–56°S (SRTM); per-dataset coverage in the data registry (167 datasets, grouped by catalog_group) and on `sources.html` |
| Clarify coverage scope across all pages | 🔶 | Homepage done; extend to `technology.html` / `for-*` pages where relevant |
| National open-data + geo portal connectors | 🔶 | 2026-09-03: 98 global sources catalogued as candidates in the Data Observatory (national portals incl. EU/US/UK/FR/DE/ES/CA/AU/JP/KR/IN/BR/MX/SA/QA/JO/MA/TN, international orgs, hazard archives, EO, climate, environment, energy, evidence registries) — URLs audited. Gradual engine wiring waves 1–2 + late wave done: GDACS multi-hazard (flood FL + volcanic VO), NASA EONET (wildfire + dust events), GEOGLOWS + USGS gauges + USGS dv history (discharge: 2 models + observed gauges), **earthquake module** (USGS ComCat + EMSC — 10 hazards), **IBTrACS cyclone archive** (prepared, last 3 seasons), **CAMS dust pipeline** (key-gated, activates with CAMS_ADS_KEY); GEM re-evaluated → stays candidate (no point API); 25/168 datasets integrated |

## Go-to-market

| Item | Status | Notes |
|---|---|---|
| Marketing workspace + daily copilot | ✅ | `marketing/` + `scripts/marketing_status.py morning/evening`; hazard→market radar live. LinkedIn post-time macOS alerts active since 2026-09-07 (`scripts/linkedin_post_alert.sh` — cron Tue/Wed/Thu 07:00 + Sun 16:00 local, notification + opens the day's draft). Outreach review alerts 2026-09-07 (`scripts/outreach_review_alert.sh` — live queue stats + opens consultants preview: one-shot Fri 09-11 17:30 `week` + Sat 09-12 10:30 `review` local; **end-of-campaigns `final` mode Wed 09-16 17:30** — per-wave report `data/campaign_final_review.txt` + replies + next decisions) |
| Daily outreach volume | ✅ | Operator decision 2026-09-07: 15/day this week → 20/day from 2026-09-14 (`DAILY_SEND_CAP=15` in `.env`; one-shot cron `data/bump_send_cap.sh` bumps it on 09-14). Fixed `sent_today_count` double-count (scheduled sends were counted twice → effective cap was ½); 13 stale pre-pivot scheduled rows (insurance/banking/investment templates) cancelled — fresh start under the compliance identity. **2026-09-12: backfill automation added** (`scripts/backfill_daily.py`, dry-run default, `--schedule`; called from `scripts/email_cron.sh` after the processor) so already-scheduled waves take their share and the queue is topped up to the cap every 5 min |
| Audience segmentation (government / insurance / investment / …) | ✅ | 34 segments in `marketing/segments/segments.json` — incl. `sustainability_compliance` (tier-A since 2026-09-06) + `eudr_operators` — matched to the site's industries hub |
| Grant classifier — EU + global companies + banks | ⬜ | Foundation exists (`marketing/eu_funding/`, 10 verified Horizon records; 17-programme registry `config/funding_knowledge.json` + live matcher `/api/v2/funding` + funding mode in `intelligence.html`). Unify: instrument × sector × eligibility × deadline × budget × fit score; expose in Commercial Center. **Strategic ruling (operator + copilot 2026-09-07)**: a general-purpose standalone "EU funding engine" = distraction (crowded grant-tech market, endless data maintenance, would dilute the 1-day-old compliance identity) — REJECTED for now. Funding stays a **funnel inside the compliance identity**: match programme → surface the climate-risk evidence the application requires → sell evidence packs; grant consultants are the bridge segment (wave already scheduled 09-14→16). Sequence: (1) finish this classifier as lead magnet now-ish, (2) measure wave results at the 09-16 final review, (3) only if demand proves out, build in Q4 an AI application assistant scoped to climate/adaptation grants ONLY (LIFE, Horizon Adaptation Mission, EIC — where our EO evidence is the differentiator; first verify SEDIA/Funding&Tenders data-reuse terms). Verified context: RRF €573B hard deadline 31-12-2026, €74B loans decommitted from non-absorption; Horizon Europe ~€93.5B |
| Newsletter (design matching the site) | ⬜ | Built on the existing mailer; own GDPR consent + unsubscribe; inherits site identity (Inter/Space Grotesk, #0EA5E9) |
| Case studies published | ✅ | First three live on the reports portal (`hydra-shield-platform/website/reports.html`): anonymized real-decision studies — bank credit file, insurance underwriting referral, CSRD/ESRS E1 disclosure. Operator-attested, no invented metrics; further studies only real, checkable and with the customer's written approval |
| Talaix Academy pilot course | 🔶 | 2026-09-05 deepening pass: 6→8 modules in `config/academy_course.json` — added "Hazard types and data sources" + "From hazard to financial impact", per-module learning objectives, case-study sections, try-it product links on every module; knowledge graph 30→42 nodes; glossary 22→30 terms; `academy.html` start panel (free-during-pilot note, progress bar, start/continue button). 2026-09-06 wave 2: 4 modules deepened (~127 min total), ESRS E1 physical-risk explainer brief published (3rd brief; EUR-Lex + EFRAG sources verified live), LinkedIn add-to-profile button on issued certificates; 31 academy/briefs tests green. 2026-09-06: Academy section added to homepage between "Who it's for" and "Reports" (dark band, 3 cards + CTA). SEO wave: static page per brief at `briefs/<id>.html` (canonical, OG, JSON-LD Article, `<base href="../">` for site chrome) generated from the registry by `scripts/build_brief_pages.py`; sitemap BRIEFS block auto-updated; briefs.js cards open static pages; freshness enforced by `tests/test_briefs_pages.py`. Next: more depth toward 4–6 h, `scripts/draft_brief.py` sourcing assistant, RSS feed, diagnostic onboarding, university partnership. **Publishing cadence (operator-approved 2026-09-06): evidence brief every 2 weeks — next due 2026-09-20 (human review before publish); framework explainer monthly — next due 2026-10-04** |
| Pricing clarification | ✅ | Tiers and prices decided 2026-08-31: `hydra-shield-platform/docs/PRICING.md` (Free / Professional €49/mo launch / Business €249/mo / Government from €4,900/yr / Enterprise from €12,000/yr; pay-per-report €19–39; pilot programme trades nominal price for publishable case studies; recorded-not-charged until billing ships). `pricing.html` published 2026-09-04; `account.html` links to it |
| Stripe billing integration | 🔶 | Backend blueprint + checkout/portal/webhook + idempotency + data model + frontend buttons + tests are done. Awaiting deploy + real Stripe secrets (`STRIPE_SECRET_KEY`, `STRIPE_PUBLISHABLE_KEY`, `STRIPE_WEBHOOK_SECRET`, `TALAIX_PUBLIC_BASE_URL`) and `scripts/setup_stripe_products.py` run against the live account. No secrets in Git. |
| "Where to invest / what to build" service | ⬜ | Core product item: positive siting intelligence (low multi-hazard risk + infrastructure + available funding) for companies, municipalities and governments; fields + environment-compatible technologies per location |
| Sustainability-report evidence | 🔶 | Platform reports already serve as physical-risk evidence annexes (CSRD/ESRS E1, TCFD/ISSB positioning); labelled screening-level until model validation completes (`docs/VALIDATION.md`) |
| Google Search Console — sitemap | 🔶 | 2026-09-06: `https://talaix.com/sitemap.xml` submitted (domain property; full URL required). File verified live (HTTP 200, valid XML, 28 URLs, referenced in robots.txt; `/sources` clean URL also 200). GSC shows initial "Couldn't fetch" — normal for fresh submissions; check it flips to Success within a few days |

## GRC compliance direction — compliance-first identity (operator decision 2026-09-06)

| Item | Status | Notes |
|---|---|---|
| Market study + 8 strategic decisions | ✅ | `hydra-shield-platform/docs/COMPLIANCE_STRATEGY.md`: 33 vendors profiled (17 CSRD/GRC suites — none but Clarity AI does physical risk; 16 physical-risk vendors — all opaque enterprise pricing), regulatory calendar verified (EUDR live since 30 Dec 2025 / 30 Jun 2026, CSRD W2 reports FY2027, simplified ESRS adopted 3 Jul 2026). Gap we occupy: transparently priced standalone evidence packs. Full study: `docs/MARKET_AND_COMPETITORS_2026-09.md` — physical-risk specialists live-verified 2026-09-12 (First Street→MSCI $120M completed 08-03, Climate X ESRS E1 page verified, no published prices anywhere re-confirmed, no new self-serve entrant; suites/EUDR sections still internal-record only) |
| Compliance-first identity pivot on site | ✅ | Hero/title/meta/JSON-LD → "Climate-risk compliance evidence for EU disclosure"; nav reordered (Compliance first); footer brand line; llms.txt rewritten; `compliance.html` gained the who-must-comply wave calendar. 2026-09-07: LinkedIn presence aligned — founder About rewritten compliance-first (delivered for manual paste), 09-08 post rewritten (ESRS E1 evidence angle), stale 09-05 slot retired |
| Marketing segments → compliance | ✅ | +2 segments (`sustainability_compliance` = new tier-A primary, `eudr_operators`) in `marketing/segments/segments.json` |
| sustainability.html sharpened as first-customer funnel | ✅ | Free applicability check now leads (anchor `#applicability`), 4-step path (scope → sample pack → €490 pilot → subscribe), deadline framing (W2 reports FY2027 / published 2028), pricing strip. **Pricing decision: tiers unchanged** (transparent published prices are the wedge) — added the free CSRD-check row to `pricing.html` |
| Corporate email signature → compliance-first | ✅ | `_SIGNATURE_LINES` in `mailer.py` (+ brand tests): "Climate-Risk Compliance Evidence for EU Disclosure / Earth Observation & Physical-Risk Intelligence / Luxembourg-based technology team" — appended to every outgoing message |
| Compliance outreach wave 1 — preview SENT, wave ready for Vultr sends | 🔶 | Operator approved 2026-09-06. `compliance_wave1.md` + `.json` (8 verified Tier-1 targets; Position Green = webform-only, operator submits manually) + template `outreach_sustainability_compliance` + `send_compliance_wave1.py` (dry-run default, gates, caps, idempotent). **Preview incident resolved same day**: preview scripts never loaded `.env` → silent outbox fallback on the Vultr host; fixed (both compliance + funder previews), regression test added, deployed. Preview then sent for real from the operator's local env (Backend: smtp → info@talaix.com; carries the new signature). 2026-09-08 LinkedIn post rewritten compliance-first (`2026-09-08-research-reproducible-analysis.md`, segment → `sustainability_compliance`, landing → `compliance.html`). Next: `send_compliance_wave1.py --send` on Vultr from 2026-09-08, 2/day staggered |
| EUDR exporters — higher-priority angle (operator directive 2026-09-12) | 🔶 | Operator ruling after the market/competitor study (`docs/MARKET_AND_COMPETITORS_2026-09.md`): the non-EU exporter / EUDR geolocation niche is the **least-contested** — raised to **top backfill priority** and presented in correspondence **from 2026-09-20 onward** via the dedicated template `outreach_eudr_exporters` (registered in `mailer._TEMPLATE_NAMES`; EUDR stays in the pool at all times so the daily 20 is never reduced; from 2026-09-20 it leads `_PRIORITY` with the dedicated template). ~39 `eudr_operators` leads in the store incl. non-EU exporters (Mewah SG, KLK MY, Amaggi BR); bottleneck remains verified contacts |
| Rolling outreach pipeline — no empty queue (operator directive 2026-09-07) | 🔶 | Sequence locked by operator: investors (09-08/09) → CSRD companies + insurance (09-10/11) → **consultants LAST** (09-14→16). **CSRD wave 1 SCHEDULED** (`csrd_companies_wave1.json`: 14 email — published sustainability/general mailboxes only, e.g. Meliá/TUI/BASF/OMV/Metlen — + 8 webform-manual + 7 IR-only holds; rows #74–#87: 10 on 09-10 + 4 on 09-11). **Insurance fresh batch SCHEDULED** (2 honest sends: Sara, UMAS; DPO mailboxes + weak fits skipped — 26 premium insurers were pre-pivot-contacted → follow-up path ~09-22). **Consultants wave 1 SCHEDULED (operator معتمد 2026-09-07)** — `build_consultants_wave1.py` → 41 entries from verified store contacts (Deloitte + QID dropped), rows #90–#130: 15/day on 09-14/15/16 when cap → 20. Generic scheduler `scripts/schedule_wave.py` (dry-run default, idempotent). Templates updated to the true 10-hazard roster. 21 wave tests green (`test_next_waves.py`). **CSRD wave 1 + insurance batch SENT (verified 2026-09-12 via operator Sent folder)**: 09-10 CSRD batch out (Meliá, TUI, BASF, OMV, Stora Enso, Verbund, Metlen, TITAN, +2) and 09-11 out (Autogrill, Inditex, Ferrovial, Jerónimo Martins + insurance Sara Assicurazioni, UMAS) — subject lines "The physical-risk evidence layer behind <company>'s disclosure" / "Location-level evidence for underwriting". **2026-09-12: daily backfill implemented** — `scripts/backfill_daily.py` (dry-run default, `--schedule`) fills each day to `DAILY_SEND_CAP` after approved waves, called from `scripts/email_cron.sh` after `process_scheduled_outreach.py`; tests green (`tests/test_backfill_daily.py`). **Needs-based multi-segment strategy (operator directive 2026-09-12)**: outreach is NOT limited to sustainability contacts — every segment whose profile matches Talaix's capabilities gets its own message (must-comply: CSRD/EBA/EIOPA/EUDR + needs-based: underwriting, loan books, assets, municipalities). Segment templates rewritten compliance-first (`outreach_insurance/banking/investment/governments/real_estate`); backfill priority: sustainability_compliance → eudr_operators → insurance → banking → real_estate → governments → investment → consultants (consultants moved LAST, operator directive 2026-09-12); research_centers held (partners, not buyers). **Workday-only sends (operator directive 2026-09-12)**: processor defers weekend rows to Monday (`_business_day` in `process_scheduled_outreach.py`, override `OUTREACH_SEND_WEEKENDS=1`); backfill schedules Mon–Fri only (`_workdays`); window stays 07–17 UTC. **Bottleneck is verified contacts, not code**: archive ~9,300 leads but only ~356 contacts in store; bulk `discover_contacts.py` runs per segment + external DeepSeek research batches (import via `scripts/import_contacts.py`, `verification: OBSERVED` required) are the unlock. **2026-09-12: DeepSeek batch 1 LIVE IN QUEUE** — 50 researched → 41 verified by `scripts/verify_import_batch.py` (21 clean, 20 source URLs corrected, 9 dropped as unfindable) → 3 dropped as duplicates (OMV emailed 09-10; Accor/Neste in CSRD wave list) → **38 contacts imported to prod** (35 new lead files). Backfill filled: Mon 09-14 = 3 LU banks + 17 CSRD-corporates (adidas, Henkel, thyssenkrupp, Heidelberg Materials, EnBW, Fraport…) = 20; Tue 09-15 = 10 consultants + 10 CSRD = 20; Wed 09-16 = 10 consultants + 1 CSRD + 6 EUDR (AAK, Holmen, Lenzing, Mondi, Nutreco, SCA) + 1 insurance (Signal Iduna) + 2 banking (BAWAG, Danske) = 20. Live crontab on mtz now includes backfill every 5 min (docker exec; crontab never called email_cron.sh — AGENTS.md updated). Server consultants wave reality: 10/day 09-15→09-28. Bulk `discover_contacts.py` running on server for consultants/insurance/banking/real_estate/governments/investment (300 each) to feed Thu 09-17 onward. Remaining queue: consultants wave + auto-backfill. **2026-09-13: first blocked-in-transit send** — the SC Taiwan banking mail went to `askhr@sc.com` (an HR intake, not a risk route), was gateway-tagged `[Newly Registered Domain][External]` and auto-cancelled as AskHR case HRC6274378; not a reply, not a bounce (guard correctly silent). Archived + analysed: `marketing/outreach/archive/2026-09-13-standard-chartered-askhr-case-cancelled.md`; lead ledger updated. Fix pending operator approval: purge the 3 stale HR/abuse contacts + extend discovery `JUNK_LOCALPARTS` with askhr-class intakes |
| GRC phase tracker | 🔶 | `docs/GRC_COMPLIANCE.md`: Phase 1 🔶 (CsrdTX + XBRL live since 2026-09-05; ESRS E1 datapoint-coverage map + E1-9 financial-effects context next), Phase 2 🔶 (funnel + wave draft done; sends after operator review). Infosec = internal trust enabler only, never a service line |

## ADEM pre-company workshop proposal (operator personal initiative — no Talaix branding)

| Item | Status | Notes |
|---|---|---|
| AI pre-company workshop proposal for ADEM (EN/DE/FR) | 🔶 | 2026-09-09: full 3-month intensive workshop dossier in `hydra-shield-platform/marketing/outreach/adem_precompany_workshop/` (`proposal_en.md`, `proposal_de.md`, `proposal_fr.md`) — trainer-as-participant model, case-by-case per specialty, AI for marketing/routine-automation/media outreach, UWV+VDAB/Actiris/Forem cross-border dimension, indicative tariff (€1,150/participant · €9,600 group · −20% pilot). **PDF attachment ready**: `ADEM_PreCompany_Workshop_Proposal.pdf` (9 pp, FR→DE→EN, 23 KB, built by `build_pdf.py` — render-verified). Paste-ready form message (3 languages) in `adem_form_message.txt`. **Submission via** https://adem.public.lu/en/support/contact.html — Subject = **"training"** (routes to Service Formation; verified against dropdown options), matricule in file-number field, email motaz3d@gmail.com, phone 661811680, country Luxembourg, PDF attached. **Follow-up plan**: if no reply in ~1 week call jobseeker line (+352) 247-88888 referencing send date; also mention proposal to personal ADEM counsellor at next appointment; frame as feeding ADEM's jobseeker business-creation track. Risk assessed: zero — Contact Center routing is the expected first step |

## Technology Park Ljubljana — incubation call (operator-manual)

| Item | Status | Notes |
|---|---|---|
| TP Ljubljana reply — call scheduling | 🔶 | 2026-09-09: Milan Lazarević (Start-up Community manager, milan.lazarevic@tp-lj.si) replied to the operator's 2026-08-12 HydraShield intro email — proposes online call **Mon 2026-09-14 or Tue 2026-09-15** re incubation/acceleration; coworker **Matej CC'd for AI/innovation**. Reply drafted (plain paste-ready) in `marketing/outreach/tp_ljubljana_reply_2026-09-09.txt`: proposes Mon 14 10:00 or Tue 15 14:00 Ljubljana time, flexible, agenda = incubation/acceleration + research partners + SI/EU R&D funding, live platform demo offered. Operator sends **Reply All from motaz3d@gmail.com** (thread continuity) |

## Luxembourg Space Agency — warm reply (operator-manual)

| Item | Status | Notes |
|---|---|---|
| LSA introductory call | 🔶 | **Call HELD 2026-09-10 with Dr. Alberto Maulu — very positive; he offered personal help.** His asks: precise satellite-data requirements, roadmap (time+money+customers), LSA-tailored pitch deck, business plan. His main message: **customers first** — site demo confused him (insurance menu too hard) → fix site navigation for first-time visitors. Academy/academic cooperation met neutral ("doesn't bring money") — don't push it. He advised joining the LU space community (operator lives in the north). **All 4 deliverables BUILT 2026-09-10**: `marketing/outreach/lsa_package/` — `Talaix_LSA_Data_Requirements.pdf` (P1–P8: Sentinel-1 SAR, GLO-30 DEM, thermal, S-3/S-5P, MCD64A1, VHR via ESA TPM + asks: CDSE guidance, vouchers, LIST/SnT validation partner, ESA BIC/LuxIMPULSE fit), `Talaix_Roadmap_24M.pdf` (Q4-26→Q3-28: LU S.à r.l. + ESA BIC app Q4-26, 5→90 customers, €850K budget split, ARR run-rate targets €150K end-27 / €400K Q3-28), `Talaix_Business_Plan.pdf`, and `marketing/outreach/lsa_pitch_deck.pdf` (`build_deeptechxl_deck.py --fund lsa`, 12 slides, LU-facing; 5 new tests green). **Thank-you email (formal, references the 4 attached PDFs): `marketing/outreach/lsa_thankyou_2026-09-10.txt`** — operator sends **Tue 2026-09-15** as reply in thread from info@talaix.com |

## ESA BIC Poland — eligibility confirmed (operator-manual)

| Item | Status | Notes |
|---|---|---|
| ESA BIC Poland reply — call to schedule | 🔶 | 2026-09-11: Michał Chwieduk (Head of ESA BIC Poland, ARP) replied to the operator's 2026-08-13 HydraShield intro — **project fits general requirements; pre-incorporation application OK** (company established at Incubation Contract signing); funding **€50K basic + up to €100K Incubation Boost** (Boost limited to top 8, after Mid-Term Review); Michał stated a Polish work permit is required at application — **moot for the operator: Finnish citizen → EU free movement, no permit needed, only residence registration** (clarification requested). Meeting offered **Wed 16.09 9:30–15:30** or **Tue 22.09 12:00–16:00**. Reply drafted (`marketing/outreach/esabic_poland_reply_2026-09-11.txt`, plain paste-ready, from motaz3d@gmail.com in-thread): proposes 16.09 10:00 or 22.09 13:00, updates positioning (rebrand → Talaix, 10 hazards, compliance-first, live platform, open-source engine), states EU/Finnish citizenship and asks which documents an EU-citizen founder provides at submission. **Strategic fork**: the 24M roadmap (LSA package) targets **ESA BIC Luxembourg + LuxIMPULSE** (founder in LU, LSA warm) — the Poland route implies relocation only (no work permit — EU/Finnish citizen); decide before committing |

## SEEIP — Sofia Tech Park (operator-manual)

| Item | Status | Notes |
|---|---|---|
| SEEIP Cohort 4/2026 application | 🔶 | Confirmation received 2026-09-10 12:58 (applied as "**HydraShield Earth Systems**" — old name; clarify the Talaix rename at the intro meeting or by email). **REQUIRED next step: book the 30-minute introductory meeting** (mandatory part of the process — booking link in the application email/portal; verify it points to an official SEEIP/Sofia Tech Park domain, the confirmation sender `test@vihraai.com` "[CHANGE ME]" is an unconfigured placeholder). Admission decision comes after the meeting |

## Funders outreach wave 1 (draft, awaiting operator review)

| Item | Status | Notes |
|---|---|---|
| Wave-1 draft — 10 funder targets (EIC, LIFE-CINEA, DG ECHO, EUSPA, EEA, Climate-KIC, MITECO, ApC Portugal, Poland MoC, BNDES) | 🔶 | `marketing/outreach/funders_wave1.md`; official recipient emails sourced 2026-08-31 |
| Preview sample email (EIC( → operator inbox | 🔶 | `scripts/send_preview_funder_wave.py` + `email_templates/outreach_funders.txt`; local run lands in `data/outbox/` — real send on Vultr; 18 mailer tests green |

## Investor track

| Item | Status | Notes |
|---|---|---|
| Global investor landscape — all 6 world regions | ✅ | `marketing/research/global_investor_landscape_2026-09.md` + **interactive HTML** (`global_investor_landscape_2026-09.html`, same folder — search/region/EU-BV/verified/Tier-A filters): 106 entities across Europe (38), APAC (15), MENA (15), NA (13), Africa (13), LatAm (12); Tier-A shortlist of 26 (LUMO Labs, HTGF, BOM/Brabant Ventures, Satgana, Munich Re Ventures, Seraphim Space, Pale Blue Dot, Faber…), outreach waves A/B/C sequenced, domicile compatibility flagged per entity, verified-ticket marks + master re-check list |
| Operator contact for investor-facing signatures | ✅ | Motaz Omarien · motaz3d@gmail.com · **+352 661811680** · talaix.com · LinkedIn: linkedin.com/in/motaz-omarien-8394359 (added 2026-09-09) — phone added to deck title + closing slides (regenerated 2026-09-07) and to be used in the DeepTechXL reply signature |
| **Next actions (operator-manual)** | 🔶 | 1) ~~Send the DeepTechXL reply~~ ✅ **DONE 2026-09-09** (operator confirmed: reply sent from motaz3d@gmail.com in the same thread with `marketing/outreach/deeptechxl_pitch_deck.pdf` attached). 2) ~~Export LinkedIn profile → PDF~~ **Approach changed 2026-09-09 (operator)**: LinkedIn-profile-first — the founder URL (linkedin.com/in/motaz-omarien-8394359) is already circulating in investor forms (Speciale, Savia), so the live profile IS the bio. Copilot drafts the full paste-ready profile kit (headline + compliance-first About + Talaix experience entry + Featured links) into `marketing/content/`; operator adds real employment history; deck Team slide then regenerates from the same text (09-07 About draft was chat-only, never saved — rewrite fresh) | 3) Submit the webform-only funds (URLs + notes in `marketing/outreach/investor_waves_abc.md`): **DONE 2026-09-09 — 6 submitted (4impact, Speciale, Seraphim, Satgana, Savia, OurCrowd), 2 dropped**: Munich Re Ventures ⛔ (no working public pitch channel — verified) + IDB Lab ⛔ (operator decision: structurally ineligible — ≥80% LAC, co-invest only; honest skip like Rubio). **Paste kit:** all approved form answers in `marketing/outreach/paste_kit.txt` (plain text, copy-ready — binding rule in root AGENTS.md). **Generic shareable deck** (`marketing/outreach/talaix_preseed_deck.pdf`, 0 DeepTechXL mentions) live at the public Drive link recorded in `investor_waves_abc.md` (verified opens without sign-in 2026-09-09) |
| Investor waves A/B/C — 15 funds SCHEDULED | ✅ | Operator read every rendered message in `investor_waves_abc_preview.html` and approved 2026-09-07. Research: official contact routes verified per fund (only literally published emails used; no guessing). Template `outreach_investor` + `marketing/outreach/investor_waves_abc.json` + `scripts/schedule_investor_waves.py` (dry-run default, idempotent) + 8 tests green. Rows #59–#73 in `scheduled_outreach`: **11 on 09-08 (07:05–09:35 UTC) + 4 on 09-09**, 15-min stagger, within cap 15/day + window 07–17 UTC (combined with Vultr compliance wave: 14 then 6). Wave A→B→C order. **Rubio excluded** (official page excludes ESG/transparency tools); PBD/SE Ventures/Main Sequence deferred (research interrupted, operator call); 100x100/Dalus/DCVC have no published route (warm-intro only). **Wave A complete (verified 09-10)**: 11 sent 09-08 + 4 sent 09-09 (Earth VC, Speciale, Mubadala, Lux Capital) — 15/15, and 0 failed/blocked across all 130 queue rows; Faber + 4impact auto-acks received same minute as sends (delivery confirmed) — logged as notes; no human replies yet. **4impact moved to the webform-only list** (auto-reply: email applications not processed; form at 4impact.vc/contact). **check_replies hardened 09-08**: plus-tagged sender aliases now match the base mailbox + intake-bot phrases added ("thank you for your contact", "we do not process any startup applications") — both acks would previously have scanned unmatched/misclassified as human replies; +2 regression tests (31 campaign/investor tests green) |
| Pitch deck for DeepTechXL (Joel George, Analyst — replied 2026-09-07 asking for the deck) | ✅ | `marketing/outreach/deeptechxl_pitch_deck.pdf` (12 slides, 16:9) built by `scripts/build_deeptechxl_deck.py`; researched: Fund I €110M, tickets €100K–€2M, KET Digital Technologies × Sustainability/Security fit, portfolio hardware-heavy (framed via Stage-2 sensor testbed). Ask: **€850K pre-seed / 24 mo** (calibrated vs Dryad €1.8M, repath €1.2M, Mitiga €1.2M, Coolset €1.5M). IP: inbreng in natura into Dutch BV + CLA on tore + BOIP/EUIPO trademark + WBSO→Innovation Box. Reply goes from motaz3d@gmail.com (thread continuity), signature Talaix |

## Repositioning — financial/investment product (operator decision 2026-09-02)

| Item | Status | Notes |
|---|---|---|
| Site IA + homepage → financial/investment identity | ✅ | Verified live 2026-09-02: new nav, money-first homepage, wildfire pages hidden/relocated (commit 5d6295b) |
| Site IA → challenge-first entry (operator-approved 2026-09-13) | ✅ | Homepage hero = one question (“What do you need to prove, screen, or decide?”) + 6 challenge cards linking to the existing pages; nav simplified to Start · Who it's for · Capabilities ▾ (full dropdown) · Pricing. Every existing page, footer group, Caddyfile redirect and sitemap URL preserved (nothing lost). Deployed via `website/index.html` + `website/js/chrome.js`; 1707-test suite green; verified live 2026-09-13. **Superseded 2026-09-14 by the clarity pass below** |
| **Clarity pass — new-visitor message + product clarity (operator-approved 2026-09-14)** | 🔶 | Driver: external review scored **product clarity 6.5/10** and **new-visitor message clarity 6/10**; the diagnosis found (a) the hero asked a question instead of stating what Talaix is, (b) ~30 competing cards on the homepage (45 links) presenting the same engine in 5 vocabularies, (c) zero concrete proof without an account (no downloadable sample), (d) contradicting hazard counts (10 homepage / 7 pricing / “6 of 6”), (e) no “who is behind this” page, (f) abstract nav labels. **Fixes shipped**: homepage hero now states what/who/format/price (“Climate-risk evidence your auditor can trace” + “Talaix turns satellite and open Earth-observation data into … evidence packs for EU disclosure … Send site coordinates; get a PDF …”) with CTAs *Check your CSRD scope — free* + *See a real sample pack*, a 3-step “How it works”, 3 challenge cards instead of 6, trust line (Luxembourg · open-source engine EUPL-1.2 · 10 hazards · 25 datasets integrated of 167 catalogued); **`capabilities.html`** now holds the full product map (compliance, insurance, siting/funding, maps, 7 report families, licensing, academy, pricing pointer); **`sample.html` + `assets/samples/talaix-sample-evidence-pack.pdf`** = the deliverable readable with no account (real Clervaux 2026-08-25 engine output, explicitly labelled “sample of the format, not a live run”; regenerated by `scripts/build_sample_pack.py`, `--check` verifies); **`about.html`** = who is behind the evidence (Luxembourg, founder-led, pre-incorporation stated honestly, open-source engine link, contact, “what we are not”); hazard counts unified to 10 everywhere and the DNSH subset now reads “6 of the 6 DNSH hazards relevant to this asset … up to 10 hazards per site”; nav relabelled to **How it works · Who it's for · Capabilities ▾ (first entry “All capabilities (one page)”) · Pricing**; `about.html` + sample pack added to the footer; sitemap + llms.txt updated (Start here block). Contract locked by `tests/test_site_clarity.py`. **Committed `3c1a42e` → CI 34856788553 green → deployed and verified live on talaix.com 2026-09-14** (capabilities.html / sample.html / about.html / sample PDF all HTTP 200; homepage hero, "How it works" nav and the sample CTA confirmed in a headless render of the live site) |
| Guided quick-path stepper on service pages | ✅ | 2026-09-13: reusable `website/js/quickpath.js` (auto-loaded from chrome.js, no-op without a mount) renders a **progressive-disclosure tunnel** via `<div class="quickpath" data-mode="hide" data-steps="id:Label,...">` — one step visible at a time, Continue/Back + "Show full page" — on CSRD, Green Finance, EUDR, Insurance, Forensics and Licensing. The full page stays below (design unchanged); CSRD's redundant "Your path" cards removed. Verified live on talaix.com/sustainability.html |
| Search-assist helper dropdown on every field | ✅ | 2026-09-13: `website/js/search-assist.js` now covers text, number and textarea; every form field (company/sector/country/supplier/plots/sites/radius/jurisdiction/title + CSRD applicability thresholds) opens a context-appropriate helper (quick picks + tips, incl. `label|value` chips). Fixed the blur-timer race that closed the dropdown immediately on field switch. Dynamic location inputs (`sec-loc-input`, `fundLocWidget_q`) covered too. 2026-09-14: universal fallback added (`EXTRA_CONFIG` + `defaultConfig` + `isLocationField`) so every text/number/textarea field on the covered pages opens a helper — never an empty box; added tailored configs for reports-builder fields, intelligence radius, map compare, forensics claim text and academy cert-id (test `test_every_field_gets_a_helper_dropdown`) |
| Sector chip redesign + footer tweaks | ✅ | 2026-09-13: sector toggle buttons (intelligence.html#sector) restyled from bare buttons to a 3-column grid of toggle cards (checkmark, blue gradient active, hover lift). Footer: "Pricing" moved from Products to About column |
| Reports engine front-and-centre | ✅ | Verified live 2026-09-02: top-level nav, finance-first order, wildfire PDFs archived |
| Environmental Licensing advisory page | ✅ | Verified live 2026-09-02: `licensing.html` HTTP 200 (advisory, not a legal permit) |
| Documented loss figures — free open sources | ✅ | Verified live 2026-09-02: NOAA NCEI integrated; `/api/v2/losses/summary` returns real figures in production; EM-DAT/DesInventar staged ingest |
| Commercial loss-DB licences (Munich Re NatCatSERVICE, Swiss Re sigma) | ⬜ | Procure after first platform revenue; registry entries marked `planned` |
| Plain-language + data-graphics layer (operator-approved 2026-09-14) | 🔶 | `css/viz.css` + `js/glossary.js` (45 plain-English term tooltips, auto-enhances acronyms site-wide, renders `glossary.html`) + `js/viz.js` (dependency-free meters/rings/bars/timelines/steps/stats via `data-viz`) — all auto-loaded from `chrome.js`. Applied: homepage (hero stats 10/167/6 + 3-step "how it works"), compliance (regulatory timeline), sustainability (CSRD "2-of-3" thresholds), green-finance + insurance (per-hazard meters), supply-chain + forensics (tree-cover rings), licensing (4-step process). Verified: full suite 1706/1707 green (the 1 failure — forensics `cannot_assess` token — fixed) + headless-Chrome DOM renders, 0 JS errors. **Wave 2 done**: phrase tooltips auto-applied site-wide (double materiality, per-peril, actuarial, physical risk, transition risk, scenario analysis, earth observation, geolocation, multi-hazard, second party opinion) + technology evidence-status legend + a glossary closure bug fixed. Remaining (low value — JS-driven or already visual): industries, pricing, reports, academy |

| **Product-line simplification — nav trimmed to 7 core doors (operator-approved 2026-09-15)** | 🔶 | Operator decision to thin the branches and sharpen the audience. `website/js/chrome.js` Capabilities dropdown trimmed 14 → 7 core doors (one-page map · compliance overview · CSRD · Green Finance DNSH · EUDR · Insurance · Maps); specialist tools (forensics, licensing, siting/funding, reports, academy, verify, sources) demoted via `nav:false` but still reachable from the footer + `capabilities.html` (nothing lost). `capabilities.html` regrouped into **Core** (the three compliance doors) vs **Specialist** (forensics/insurance/siting/funding/sector/licensing). Homepage CTA shortened ("See all capabilities"). Contract: `tests/test_site_clarity.py` + `test_capabilities_dropdown_is_trimmed_to_the_core_doors`. Web-layer only — no engine change, no tore sync. Committed + pushed → CI/deploy pending. |

## Sellability & first customer (operator goal 2026-09-14)

| Item | Status | Notes |
|---|---|---|
| Purchase path completed on the intent pages | 🔶 | **Gap found by audit**: the four highest-intent pages (green-finance, insurance, sustainability, sample) had **no purchase CTA at all**; the only buy route was `pricing.html`; a guest clicking there was bounced to a bare sign-in screen; and **the site ignored Stripe's return flags** — `account.html?purchased=report&subscribed=1` had no handler anywhere, so a buyer who had just paid landed on a plain account page with no confirmation and no way to collect the pack. **Shipped**: `website/js/buy.js` — the one reusable purchase block (live price from `/api/v2/billing/config`, "what you receive", the 3 steps, invoice-by-email route, guest path that stores `hs_buy_intent` + `hs_buy_location` and returns after sign-in), auto-loaded from `chrome.js`, mounted on green-finance (decision €19), insurance (scientific €39), sample (decision) and sustainability (Professional €49/mo); `website/js/postpurchase.js` on `account.html` handling `purchased=report` (→ generate the pack, location pre-filled via `reports.html?location=…`), `subscribed=1`, `checkout=cancelled`, and resuming an interrupted purchase; `pricing.html` gained a "How buying works" panel (3 steps, Stripe, invoices, VAT, cancellation). Contract: `tests/test_buy_path.py` (cross-checks the JS endpoints against `src/dashboard/billing.py`). **Committed `3c1a42e` → CI 34856788553 green → deployed and verified live 2026-09-14**: on talaix.com the buy panel renders on green-finance / insurance / sample / sustainability and pulls the **live** prices from `/api/v2/billing/config` (headless render shows "Buy €19", "Buy €39", "Buy €49" + the invoice route) — the only untested step is a real card payment |
| Production billing reality (verified live 2026-09-14) | ✅ | `/api/v2/billing/config` → `billing_enabled: true` with a **live** publishable key and real prices: report_decision €19 · report_scientific €39 · professional €49/mo (€490/yr) · business €249/mo (€2,490/yr) · seat €25/mo. `/api/v2/billing/checkout` and `/checkout/report` both require a signed-in account (401 → redirect). **PLAN was stale** ("awaiting deploy + real secrets", "recorded-not-charged until billing ships") — Stripe is live; the site can take money today |
| ✅ **Decision 1 — the paid reports are entitlement-gated (shipped 2026-09-14)** | ✅ | Operator approved; verified live before the fix that `/api/report?type=decision|scientific` handed real PDFs (31,632 / 35,199 B) to anyone with no auth. **Shipped**: `BillingStore.has_purchase()` (reads only `status='completed'`, idempotent) + `_report_entitlement_gate()` in `src/dashboard/api.py` — `simple` stays free for everyone (the published free-tier promise), `decision` (€19) and `scientific` (€39) require an active subscription or a completed purchase of that exact pack, admins always pass, and the gate runs **before** any analysis work so an unentitled request is answered immediately. A refusal is 402 JSON carrying `buy_url` / `free_report_url` / `sign_in_url`, or a branded purchase page for a browser request. Contract: `tests/test_report_entitlement.py` (15 tests). **Verified live in production 2026-09-14 (commit `af99872`, run 34868015636, completed success)**: anonymous `decision` → **402** JSON (`purchase_required` + buy/free/sign-in URLs), `scientific` → **402**, `simple` → **200 `application/pdf`** (free tier intact), and a browser request for a paid pack → **402 `text/html`** branded purchase page. **Still open (related)**: `/api/v2/report-builder/` PDF export is gated only to `registered`, so a free account can still assemble a custom PDF — decide whether the builder should require a subscription too |
| ✅ **Decision 2 — FIRMS key live in production (verified 2026-09-14)** | ✅ | Problem: `/api/health` → `firms_configured: false`, and the live wildfire analysis returned `active_fires: unavailable — "NASA FIRMS API key not configured (set FIRMS_MAP_KEY)"`, so the flagship hazard and the homepage live monitor ran without observed-fire data. **Done**: `info@talaix.com` was already registered with NASA FIRMS; the MAP_KEY was re-sent to that inbox; the operator supplied it; I validated it directly against the FIRMS area API (HTTP 200) and set it as the repo secret `FIRMS_MAP_KEY` (2026-09-14T16:28Z) via stdin — never in a command line or in Git. The gate deploy had been created *before* the secret existed (its guard was false), so the AI-commit push `3e31a09` (run 34869306868) carried it through `set_env`. **Verified live**: `/api/health` → `firms_configured: true`, and the wildfire analysis returns `active_fires: kind observed · claim OBSERVED · quality ok · available: true` (0 active fires in the Luxembourg box today — correct) — the flagship hazard and the homepage live monitor now carry real observed-fire data |
| ✅ **Decision 3 — the two service promises are confirmed (operator 2026-09-14)** | ✅ | Operator approved the copy as written: (a) packs we prepare by hand (portfolio scope, extra locations, custom annexes) are "delivered within one business day", and (b) organisations can request an invoice by email. Both are stated in `js/buy.js`, `js/postpurchase.js` and the pricing "How buying works" panel. **They are now service commitments and must stay keepable** |
| 🔶 **Cost reduction — free, in-house AI provider (implemented 2026-09-14)** | 🔶 | Operator directive: "if there is free software technology, implement it — we want everything in-house here". **Audit of paid software subscriptions in the codebase**: the only one is the **AI gateway** (Kimi — `KIMI_API_KEY` + `AI_DAILY_CALL_CAP`); Stripe is a payment rail (not replaceable by software, but the invoice route added today avoids its fees for early customers) and Vultr is hosting, not software. **Shipped**: a `local` provider in `src/ai/gateway.py` — any OpenAI-compatible server on our own hardware (Ollama, llama.cpp `--server`, vLLM, LM Studio), defaulting to `http://127.0.0.1:11434/v1/chat/completions` and overridable with `LOCAL_AI_BASE_URL` / `LOCAL_AI_MODEL_CHEAP` / `LOCAL_AI_MODEL_STRONG`; **no API key**, no Authorization header, prompts never leave the machine; usage is still logged and the daily cap still applies (free ≠ unmeasured); the hosted providers still require their key. 8 new tests (27 in `tests/test_ai_gateway.py`). **To switch** (operator decision — I deliberately did not flip it, because it would disable AI features until a local server exists): run Ollama on the machine that should do the inference, then set `KIMI_PROVIDER=local` on the server, plus `LOCAL_AI_BASE_URL=http://<that-machine>:11434/v1/chat/completions` if the model server is not the API host |
| **Report language — business language, no internal identifiers (operator directive 2026-09-14)** | ✅ | Driver: the operator reviewed the published sample pack and found raw internal status codes in the compliance-mapping table (`covered_by_evidence`, `not_covered (declared)`). **Shipped**: a business-language label layer — `coverage_label()` in `src/climate/sustainability.py` (covered_by_evidence → "Covered by evidence", partial → "Partially covered", not_covered → "Not covered — declared boundary"), `fit_band_label()` in `src/climate/solutions.py`, `component_label()` in `src/dashboard/ignition.py`, and a shared `human_label()` in the new `src/dashboard/report_language.py` (report provenance keys, mapped-facility keys, generic statuses). Machine values stay the API/SDK contract; every customer-facing document prints the label. Applied to the sample pack PDF (regenerated), the Sustainability Evidence Report PDF, the paid report PDF, the insurance profile PDF, the website pages (reports/sustainability/sample), the ESRS E1 explainer brief (registry + regenerated page), the academy content and the coverage-map chips. Contract: `tests/test_report_language.py` scans the shipped PDFs/MD/pages for `snake_case` identifiers; `tests/test_ignition.py` + `tests/test_reports_portal.py` updated. **Second defect found and fixed in the same pass**: reportlab table cells were being handed pre-escaped strings, so escaped markup printed literally (the DNSH checklist showed `&amp;` between risk classes, the ESRS E3 row showed `&amp;`) and long cell text never wrapped — `_kv_table` / `_checklist_table` (`src/dashboard/verification_report.py`) and the coverage/hazard tables (`src/dashboard/sustainability_report.py`) now wrap cells in `Paragraph` via a shared `_as_cell` helper; verified by rendering (zero entity leaks) and pinned in `tests/test_report_language.py`. Same label applied to the SDK CLI frameworks table. **Committed `e841c13` → CI run `34878572361` green (test job success) → deployed and verified live on talaix.com 2026-09-14**: the live sample pack PDF (HTTP 200, 5,129 B) extracts with zero `snake_case` identifiers and shows "Covered by evidence / Partially covered / Not covered — declared boundary"; reports.html, sustainability.html, sample.html and the ESRS E1 brief page all carry business labels; `/api/health` → `status: ok`. Engine files mirrored and pushed to tore (`fd341ee`). |



## Operating rules (binding)

- No fabricated content: leads, grants, case studies and prices enter only
  from real, checkable sources or an explicit operator decision.
- Every deploy: push to `main` → GitHub Actions tests (784) → Vultr.
- Server secrets live only in `/opt/hydrashield/.env` (chmod 600).
