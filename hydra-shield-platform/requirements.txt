# HydraShield runtime dependencies.
# Versions pinned to those verified against the current test suite.
# For development/testing, also install requirements-dev.txt.

# Data Science & ML
scikit-learn==1.9.0
pandas==3.0.5
numpy==2.5.2

# GIS & Earth Observation (optional for core modules; see pyproject.toml [gis])
geopandas>=0.14
shapely>=2.0
eolearn>=1.5

# Jupyter (optional; see pyproject.toml [notebook])
notebook>=7.0
jupyterlab>=4.0
matplotlib>=3.8
