# Events radar ledger

One JSON file per event, following `schema.json`. **Empty by design** —
events are committed only from the official event page, live-checked at
`date_checked`. The copilot's `events` command ranks watching/attending
events by date and relevance.
